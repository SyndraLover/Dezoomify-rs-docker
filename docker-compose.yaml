services:
#  gluetun:
#    restart: always
#    image: ghcr.io/qdm12/gluetun:latest
#    container_name: gluetun
#    cap_add:
#      - NET_ADMIN
#    devices:
#      - /dev/net/tun:/dev/net/tun
#    ports:
#      - 9420:9420
#    volumes:
#      - ./gluetun:/gluetun
#    environment:
#      - VPN_SERVICE_PROVIDER=mullvad

  dezoomify-rs:
    user: 1000:1000
    image: ghcr.io/syndralover/dezoomify-rs:latest
    container_name: dezoomify-rs
    restart: "unless-stopped"
#### COMMENT Ports when using gluetun
    ports:
      - "9420:9420"
    # MAX is 8k by default
    #environment:
      #- MAX_WIDTH=
      #- MAX_HEIGHT=
      #- ALWAYS_LARGEST=True
    volumes: 
      - ./dezoomify:/app/export
      - dezoomify_import:/app/import

### UNCOMMENT when using gluetun
#    network_mode: "service:gluetun"